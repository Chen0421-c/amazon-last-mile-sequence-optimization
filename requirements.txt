# Runtime dependencies for the cleaning pipeline.
# The pipeline intentionally uses only the Python standard library so it can run
# in constrained Colab/Drive environments without loading large JSON files into memory.
